/** @type {import('next').NextConfig} */
// const nextConfig = {
//   reactCompiler: true,
// };

// export default nextConfig;


const nextConfig = {
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
